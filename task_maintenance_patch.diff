--- a/salsa_app_extracted/lib/blocs/task_maintenance/task_maintenance_bloc.dart
+++ b/salsa_app_extracted/lib/blocs/task_maintenance/task_maintenance_bloc.dart
@@ -4,22 +4,28 @@
 import 'package:salsa/blocs/task_maintenance/task_maintenance_repository.dart';
 import 'package:salsa/blocs/task_maintenance/task_maintenance_state.dart';
 
-class TaskMaintenanceBloc extends Bloc<SearchPO, POSearchState> {
+class TaskMaintenanceBloc extends Bloc<TaskMaintenanceEvent, TaskMaintenanceState> {
   final TaskMaintenanceRepository repository;
 
-  TaskMaintenanceBloc({required this.repository}) : super(POSearchInitial()) {
-    on<SearchPO>(_onSearchPO);
+  TaskMaintenanceBloc({required this.repository}) : super(TaskMaintenanceInitial()) {
+    on<TaskMaintenanceSearchRequested>(_onSearchPO);
   }
 
-  Future<void> _onSearchPO(SearchPO event, Emitter<POSearchState> emit) async {
-    emit(POSearchLoading());
+  Future<void> _onSearchPO(TaskMaintenanceSearchRequested event, Emitter<TaskMaintenanceState> emit) async {
+    emit(TaskMaintenanceLoading());
     try {
       // Panggil method repository yang baru
       final results = await repository.searchTransactions(
           event.transNo, event.maintenanceBy, event.taskType);
-      emit(POSearchSuccess(results));
+      
+    if (results.isEmpty) {
+      emit(TaskMaintenanceEmpty());
+    } else {
+      emit(TaskMaintenanceSuccess(results));
+    }
+
     } catch (e) {
-      emit(POSearchFailure(e.toString()));
+      emit(TaskMaintenanceFailure(e.toString()));
     }
   }
 }

--- a/salsa_app_extracted/lib/blocs/task_maintenance/task_maintenance_event.dart
+++ b/salsa_app_extracted/lib/blocs/task_maintenance/task_maintenance_event.dart
@@ -1,13 +1,13 @@
-abstract class POSearchEvent {}
+abstract class TaskMaintenanceEvent {}
 
 enum MaintenanceType {
   service, // Untuk layanan service
   cuci,    // Untuk layanan cuci
 }
 
-class SearchPO extends POSearchEvent {
+class TaskMaintenanceSearchRequested extends TaskMaintenanceEvent {
   final String transNo;
   final String maintenanceBy;
   final MaintenanceType taskType;
-  SearchPO(this.transNo, this.maintenanceBy, this.taskType);
+  TaskMaintenanceSearchRequested(this.transNo, this.maintenanceBy, this.taskType);
 }
--- a/salsa_app_extracted/lib/blocs/task_maintenance/task_maintenance_state.dart
+++ b/salsa_app_extracted/lib/blocs/task_maintenance/task_maintenance_state.dart
@@ -1,24 +1,26 @@
 import 'package:equatable/equatable.dart';
 import '../../models/task_maintenance/task_maintenance_model.dart';
 
-abstract class POSearchState extends Equatable {
+abstract class TaskMaintenanceState extends Equatable {
   const POSearchState();
   @override
   List<Object> get props => [];
 }
-class POSearchInitial extends POSearchState {}
-class POSearchLoading extends POSearchState {}
-class POSearchSuccess extends POSearchState {
+class TaskMaintenanceInitial extends POSearchState {}
+class TaskMaintenanceLoading extends POSearchState {}
+class TaskMaintenanceSuccess extends POSearchState {
   final List<TransactionSuggestion> suggestions;
   const POSearchSuccess(this.suggestions);
 
   @override
   List<Object> get props => [suggestions];
 }
-class POSearchFailure extends POSearchState {
+class TaskMaintenanceFailure extends POSearchState {
   final String message;
   const POSearchFailure(this.message);
 
   @override
   List<Object> get props => [message];
-}+}
+
+class TaskMaintenanceEmpty extends TaskMaintenanceState {}

--- a/salsa_app_extracted/lib/blocs/task_maintenance/task_maintenance_repository.dart
+++ b/salsa_app_extracted/lib/blocs/task_maintenance/task_maintenance_repository.dart
@@ -13,11 +13,12 @@
       final params = {
         'trans_no': transNo,
         'maintenance_by': maintenanceBy,
-        'task_type': taskType.toString().split('.').last,
+        'task_type': taskType.name,
       };
 
       Uri uri = getUrl(pathUrl: 'task_maintenance', params: params);
-      final response = await http.get(uri);
+      final response = await http.get(uri)
+    .timeout(const Duration(seconds: 15));
 
       print(uri);
 
@@ -28,9 +29,13 @@
           return results
               .map((json) => TransactionSuggestion.fromJson(json))
               .toList();
-        } else {
-          throw Exception(body['message'] ?? 'Transaksi tidak ditemukan');
-        }
+        } else if (response.statusCode == 401) {
+      throw Exception('Sesi berakhir, silakan login ulang.');
+    } else if (response.statusCode >= 500) {
+      throw Exception('Server sedang bermasalah. Coba beberapa saat lagi.');
+    } else {
+      throw Exception('Gagal memuat data (Kode: ${response.statusCode}).');
+    }
       } else {
         throw Exception(
             'Gagal memuat data PO. Status Code: ${response.statusCode}');

--- a/salsa_app_extracted/lib/screens/task_maintenance/components/task_maintenance_body_mobile.dart
+++ b/salsa_app_extracted/lib/screens/task_maintenance/components/task_maintenance_body_mobile.dart
@@ -44,17 +44,21 @@
 
     // Logika untuk mendeteksi tipe transaksi
     late MaintenanceType taskType;
-    if (transNo.toUpperCase().contains('SC')) {
-      taskType = MaintenanceType.service;
-    } else if (transNo.toUpperCase().contains('PO') ||
-        transNo.toUpperCase().contains('SRO')) {
-      taskType = MaintenanceType.cuci;
-    } else {
-      ScaffoldMessenger.of(context).showSnackBar(
-        const SnackBar(content: Text('Format nomor transaksi tidak dikenali.')),
-      );
-      return;
-    }
+    final upper = transNo.trim().toUpperCase();
+final isSC  = RegExp(r'^(SC)[\/-]').hasMatch(upper) || upper.startsWith('SC/');
+final isPO  = RegExp(r'^(PO|SRO)[\/-]').hasMatch(upper);
+
+late MaintenanceType taskType;
+if (isSC) {
+  taskType = MaintenanceType.service;
+} else if (isPO) {
+  taskType = MaintenanceType.cuci;
+} else {
+  ScaffoldMessenger.of(context).showSnackBar(
+    const SnackBar(content: Text('Format nomor tidak dikenali.')),
+  );
+  return;
+}
 
     context.read<TaskMaintenanceBloc>().add(
           SearchPO(transNo, widget.userData['maintenance_by']!, taskType),
